export interface Usuarios {
    // crear estructura (con datos del json)
    user: string;
    password: string;
}


export interface Datos{
    name: string;
    email: String;
    user: string; // key de acceso
}